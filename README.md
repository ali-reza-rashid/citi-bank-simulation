# citi-bank-simulation

App.java collects the datapoints into a queue, consisting of the price and timestamp, while datapoints.py collects them into a visual graph, given the Citi program's constraints of input as a string for the datapoints.py file.
